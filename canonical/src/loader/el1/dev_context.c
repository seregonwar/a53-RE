/* EL1 dev_context init.
 * No independently addressable function body was recovered for this unit.
 * The EL1 vector table (el1_vector.S) handles exception dispatch;
 * dev_context setup at EL1 is delegated to EL3 via SMC #0/SMC #1.
 */
#include "a53_context.h"
