/* EL2 dev_context init.
 * No independently addressable function body was recovered for this unit.
 * The EL2 vector table (el2_vector.S) handles exception dispatch;
 * dev_context setup at EL2 is delegated to EL3 via SMC #2.
 */
#include "a53_context.h"
